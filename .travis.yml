dist: trusty
language: node_js
node_js:
  - "10"
  - "12"
  - "13"
  - "14"
install:
  - npm install
script: npm run lint && npm run build && npm run build-lib && npm test
cache:
  npm: false
