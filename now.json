{
    "version": 2,
    "builds": [
        {
            "src": "package.json",
            "use": "@now/static-build",
            "config": {
                "distDir": "storybook"
            },
            "scripts": {
                "build": "npm run build-storybook"
            }
        }
    ]
}
